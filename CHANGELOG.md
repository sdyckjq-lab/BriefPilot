# CHANGELOG

BriefPilot 版本记录使用 `MAJOR.MINOR.PATCH.MICRO`。

每条记录按 `YYYY-MM-DD` 标注，并优先说明用户能获得什么。

## [0.3.0.0] - 2026-05-01
### Added
- 新增 `/bp-review` 入口，让用户生成第一版后能用一个清楚命令回到 BriefPilot 做结果评审。
- 新增 AI 搜索官网官方 demo 证据包，记录原始输入、第一版、评审、第二版和是否人工修改。
- README 增加证据链接，直接指向对比演示、demo 证据、第一版、评审和第二版结果。

### Changed
- 命令包现在包含 `/briefpilot`、`/bp`、`/bp-review` 和 `/briefpilot-upgrade` 四个入口。
- 官方 demo 校验会检查证据包完整性和公开前脱敏要求。

## [0.2.0.0] - 2026-04-27
### Added
- 新增 `START_HERE.md` 和可直接复制的 `design-spec.md`，普通用户可以先按一个清楚入口完成设计工具交接。
- 新增用户流程校验，确保示例包保留一个首选复制动作、中文优先说明、设计系统路径一致性和评审下一步建议。
- 新增评审下一步输出，未通过结果会明确推荐直接修复、外部工具修改提示，或修订 spec 后重新生成。
- 新增能力边界说明，区分可确认后直接修复的本地文件、只能评审的截图/摘要，以及需要回到外部工具的设计稿。

### Changed
- 平台提示词现在是可选导出；不确定目标工具时，默认复制 `design-spec.md`，不再把三类提示词当作第一步。
- Review 默认只审查并保存建议，不会在用户只说“评审”“看看”“检查”时修改 brief、生成结果或本地代码。
- 示例和命令检查已同步到新的交接流程，`/briefpilot` 和 `/bp` 都会验证同一套用户路径。

### Fixed
- `design-spec.md` 导出到新目录时会自动创建目录，并保留嵌套 `DESIGN.md` 路径标签。
- 选择修订 spec 后重新生成时，评审结果必须指向修订后的 spec，避免用户继续复制旧说明。
- `review-next-actions.md` 现在会和评审 JSON 对齐，防止用户看到过期的下一步建议。

## [0.1.0.0] - 2026-04-27
### Added
- 首个明确版本的 BriefPilot Skill 包，包含 `/briefpilot`、`/bp` 和 `/briefpilot-upgrade` 三个入口。
- 支持把模糊页面需求整理成设计 brief、`DESIGN.md`、三类目标提示词和评审清单。
- 支持生成结果评审闭环，并能按评审结论导出修改或重生成提示。
- 支持内置 `DESIGN.md` 质量检查；没有 Node/npm 时也能完成基础验证。
- 发布和打包相关脚本支持显式 JSON 输出，方便代理和自动化读取结果。

### Changed
- 命令包现在保留版本和更新记录，方便升级或修复时说明当前包来自哪一版。
- 打包和校验现在要求可验证的 Git 来源，并会用可解析错误报告处理损坏的源码文件或安装记录。
