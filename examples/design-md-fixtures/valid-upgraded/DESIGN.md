---
version: alpha
name: Valid Fixture
description: Compact upgraded DESIGN.md fixture.
colors:
  background: "#F8FAFC"
  surface: "#FFFFFF"
  text: "#111827"
  muted_text: "#475569"
  border: "#D8DEE8"
  primary_action: "#2563EB"
  success: "#0F766E"
  warning: "#B45309"
  danger: "#B91C1C"
  info: "#0E7490"
typography:
  display:
    fontFamily: Inter
    fontSize: 44px
    fontWeight: 650
    lineHeight: 1.1
  title:
    fontFamily: Inter
    fontSize: 30px
    fontWeight: 650
    lineHeight: 1.2
  heading:
    fontFamily: Inter
    fontSize: 22px
    fontWeight: 650
    lineHeight: 1.25
  body:
    fontFamily: Inter
    fontSize: 16px
    fontWeight: 400
    lineHeight: 1.6
  caption:
    fontFamily: Inter
    fontSize: 13px
    fontWeight: 400
    lineHeight: 1.45
  label:
    fontFamily: Inter
    fontSize: 14px
    fontWeight: 600
    lineHeight: 1.3
rounded:
  sm: 4px
  md: 8px
  lg: 12px
spacing:
  xs: 4px
  sm: 8px
  md: 16px
  lg: 24px
components:
  button_primary:
    backgroundColor: "{colors.primary_action}"
    textColor: "#FFFFFF"
    borderColor: "{colors.primary_action}"
    rounded: "{rounded.md}"
---

# Valid Fixture DESIGN.md

## Overview

This AI product landing page proves a product with a visible product surface, demo path, and trial CTA.

## Visual Theme

Use restrained product proof, conversion clarity, and trust hierarchy.

## Colors

Use role-based colors for background, surface, text, muted text, border, primary action, success, warning, danger, and info.

## Typography

Use display, title, heading, body, caption, and label levels.

## Layout

Use a product-led landing layout with hero proof, workflow, trust, and conversion sections.

## Elevation & Depth

Use subtle panel depth and borders.

## Shapes

Use 8px default radius.

## Components

Buttons, source chips, and cards define default, hover, pressed, disabled, focus, error, loading, empty, success, and unavailable states.

## Responsive Behavior

Desktop, tablet, and mobile layouts must preserve keyboard order, visible focus, 44px touch targets, and readable wrapping.

## Motion & Feedback

Use restrained motion, static reduced motion fallbacks, loading feedback, success feedback, and error feedback.

## Content Voice

Use concrete, trustworthy, product-led copy.

## Do's and Don'ts

Do show product proof and source-backed demo behavior. Don't invent logos or metrics.

## Reference Direction

Reference direction: `ai_product_landing_page` - AI product landing page.

Use mechanisms only, not copied brands.

## Agent Guidance

Preserve the tokens, product proof, conversion path, focus behavior, touch behavior, and no-copy boundary.
